Future route-level page templates can live here if the flat HTML files are migrated into a generated or component-based structure.

Current production pages remain at the repository root for static hosting compatibility.
