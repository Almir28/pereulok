document.documentElement.classList.add('is-loading');
window.addEventListener('load',()=>{setTimeout(()=>{document.body.classList.add('is-loaded');document.documentElement.classList.remove('is-loading')},220)});
(function(){const input=document.querySelector('[data-sport-filter]');const cards=[...document.querySelectorAll('[data-card]')];const chips=[...document.querySelectorAll('[data-chip]')];if(!input&&!chips.length)return;let active='all';function apply(){const q=(input?.value||'').trim().toLowerCase();let shown=0;cards.forEach(card=>{const hay=(card.dataset.search||card.textContent).toLowerCase();const cat=card.dataset.cat||'all';const okCat=active==='all'||cat.split(/\s+/).includes(active);const okText=!q||hay.includes(q);const ok=okCat&&okText;card.style.display=ok?'flex':'none';if(ok)shown++});const empty=document.querySelector('[data-empty]');if(empty)empty.style.display=shown?'none':'block'}input?.addEventListener('input',apply);chips.forEach(chip=>chip.addEventListener('click',()=>{chips.forEach(c=>c.classList.remove('active'));chip.classList.add('active');active=chip.dataset.chip;apply()}));apply()})();