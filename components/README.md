Reusable frontend component templates can live here as the marketplace grows.

Current Apple Gift Card rendering is implemented in `js/agc-ui.js` so it can run on static GitHub Pages without a build step.
